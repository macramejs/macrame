

const plugin = {
    install(app) {
        //Object.defineProperty(app.config.globalProperties, '$inertia', { get: () => Inertia })
    
        // app.mixin(remember)
        // app.component('LitForm', Form);
    }
}

export {
    plugin
}